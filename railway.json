{
  "$schema": "https://json.schemastore.org/railway.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd leadsync-backend && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "cd leadsync-backend && npm start",
    "restartPolicyMaxRetries": 5
  }
}
