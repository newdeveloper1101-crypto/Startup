{
  "$schema": "https://json.schemastore.org/railway.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd leadsync-backend && npm run build && npm start",
    "restartPolicyMaxRetries": 5
  }
}
